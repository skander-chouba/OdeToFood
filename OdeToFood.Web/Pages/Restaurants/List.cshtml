@page
@model OdeToFood.ListModel
@{
    ViewData["Title"] = "List";
}

<h1>Restaurants</h1>



@if (Model.Message != null)
{
    <div class="alert alert-danger">@Model.Message</div>
}

<form method="get">
    <div class="form-group">
        <div class="input-group">
            <input type="search" class="form-control" asp-for="SearchTerm" />
            <span class="input-group-btn">
                <button class="btn btn-default">
                    <i class="fa fa-search" aria-hidden="true"></i>
                </button>
            </span>
        </div>
    </div>
</form>
<div class="container">




    @*<table class="table">
        @foreach (var restaurant in Model.Restaurants)
        {
            <tr>
                <td>@restaurant.Name</td>
                <td>@restaurant.Location</td>
                <td>@restaurant.Cuisine</td>
                <td><a asp-page="./Detail" asp-route-restaurantId="@restaurant.Id" class="btn btn-lg"><i class="fa fa-search-plus"></i></a></td>
                <td><a asp-page="./Edit" asp-route-restaurantId="@restaurant.Id" class="btn btn-lg"><i class="fas fa-edit"></i></a></td>
                <td><a asp-page="./Delete" asp-route-restaurantId="@restaurant.Id" class="btn btn-lg"><i class="fas fa-trash"></i></a></td>
            </tr>
        }

    </table>*@
    @foreach (var restaurant in Model.Restaurants)
    {
        <partial name="_Summary" model="restaurant" />
    }
    <a asp-page="./Edit" class="btn btn-primary btn-lg">Create New</a>
</div>

